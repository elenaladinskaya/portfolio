.thumbs__link:hover {
  .thumbs__img {
    outline: 3px solid #DC9322;
    filter: contrast(.7);
  }
}

.thumbs__img {
  outline: 3px solid transparent;
  max-width: 100%;
  transition: outline .3s;
}