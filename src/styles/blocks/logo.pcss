.logo {
  color: $text-color;
  font-size: 36px;
  font-weight: 800;
  text-decoration: none;
  transition: color .3s;
  z-index: 1000;

  @include phones {
    z-index: 0;
  }

  &:hover {
    color: $links-hcolor;
  }
}